name: Documentación Automática al Merge

on:
  push:
    branches: [main]

jobs:
  generate-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          
      - name: Generate Documentation from Code
        run: |
          echo "📚 Generando documentación desde código fuente..."
          php generate-docs.php
      
      - name: Upload Documentation
        uses: actions/upload-artifact@v4
        with:
          name: api-documentation
          path: docs-output/
          retention-days: 90